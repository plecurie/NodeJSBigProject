image: node:latest

stages:
  - build
  - test
  - deploy

cache:
  paths:
    - node_modules/

build:
  stage: build
  before_script:
    - npm install
  script:
    - npm run build
  artifacts:
    paths:
      - dist/
      - node_modules

test:
  stage: test
  script:
    - npm run test
  needs:
    - job: build
      artifacts: true

deploy:
  stage: deploy
  before_script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - 'which sshpass || ( apt-get update -y -qq && apt-get install sshpass -y -qq )'
    - eval $(ssh-agent -s)
    - mkdir -p ~/.ssh
    - ssh-add <(echo "$SSH_PRIVATE_KEY")
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
  script:
    - sshpass -V
    - export SSHPASS=$SSH_PASS
    - sshpass -e ssh -p22 root@vps808693.ovh.net "mkdir /home/scala/htdocs/api/_tmp/"
    - sshpass -e scp -P22 -r ./ root@vps808693.ovh.net:/home/scala/htdocs/api/_tmp
    - sshpass -e ssh -p22 root@vps808693.ovh.net "mv /home/scala/htdocs/api/current /home/scala/htdocs/api/_old && mv /home/scala/htdocs/api/_tmp /home/scala/htdocs/api/current"
    - sshpass -e ssh -p22 root@vps808693.ovh.net "rm -rf /home/scala/htdocs/api/_old"
    - sshpass -e ssh -p22 root@vps808693.ovh.net "node current/dist/index.js"
  needs:
    - job: build
      artifacts: true
  only:
    - master