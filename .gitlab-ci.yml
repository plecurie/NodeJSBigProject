image: node:latest

#services:
#  - elassandra:latest

stages:
  - test
  - build
  - deploy

cache:
  paths:
    - node_modules/

test:
  stage: test
  before_script:
    - npm install
  script:
    - npm run test

build:
  stage: build
  before_script:
    - npm install
  script:
    - npm run build

deploy:
  stage: deploy
  artifacts:
    paths:
      - build/
  only:
    - master
  before_script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - mkdir -p ~/.ssh
    - eval $(ssh-agent -s)
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
  script:
    - ssh-add <(echo "SSH_PRIVATE_KEY")
    - ssh -i ~/.ssh/id_rsa.pub -p22 root@vps808693.ovh.net "mkdir /var/www/_tmp"
    - scp -i ~/.ssh/id_rsa.pub -P22 -r build/* root@vps808693.ovh.net:/var/www/_tmp
    - ssh -i ~/.ssh/id_rsa.pub -p22 root@vps808693.ovh.net "mv /var/www/scala-api /var/www/_old && mv /var/www/_tmp /var/www/scala-api"
    - ssh -i ~/.ssh/id_rsa.pub -p22 root@vps808693.ovh.net "rm -rf /var/www/_old"