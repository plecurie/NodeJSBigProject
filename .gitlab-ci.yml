image: node:latest

#services:
#  - elassandra:latest

stages:
#  - test
#  - build
  - deploy

cache:
  paths:
    - node_modules/

#test:
#  stage: test
#  before_script:
#    - npm install
#  script:
#    - npm run test
#

#build:
#  stage: build
#  before_script:
#    - npm install
#  script:
#    - npm run build

deploy:
  stage: deploy
  artifacts:
    paths:
      - build/
  only:
    - master
  before_script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'
    - eval $(ssh-agent -s)
    - mkdir -p ~/.ssh
    - ssh-add <(echo "$SSH_PRIVATE_KEY")
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config'
  script:
    # - ssh scala@vps808693.ovh.net echo "$SSH_PRIVATE_KEY" >> .ssh/authorized_keys
    - ssh -p22 scala@vps808693.ovh.net "cd htdocs/api/ && mkdir _tmp/"
    - scp -P22 -r build/* scala@vps808693.ovh.net:htdocs/api/_tmp
    - ssh -p22 scala@vps808693.ovh.net "mv htdocs/api/current htdocs/api/_old && mv htdocs/api/_tmp htdocs/api/current"
    - ssh -p22 scala@vps808693.ovh.net "rm -rf htdocs/api/_old"